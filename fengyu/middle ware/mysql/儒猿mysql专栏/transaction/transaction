
####
多个事务对(缓存页里的)同一条数据进行更新或查询，产生的问题

脏写：
事务A改了值，未提交，事务B再次修改了值，A回滚后，B修改的值也没了。
脏读：
事务A改了值，未提交，事务B查询了值，A回滚后，B再次查询，值没了


#### 读未提交
一个事务查询或者更新了另一个还没有提交事务更新过的数据，未提交的事务一旦回滚，更新或者查询的数据就不一致了。读未提交会产生脏读、脏写问题


#### 读已提交
A事务开启后，值被B、C事务分别更新并提交，A在事务期间查询到的值发生了变化。这种情形叫做，不可重复读。就是一个事务多次读取一个数据，每次得到的结果不一致。

#### 可重复读






